node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
en.example

# Runtime data (SQLite DB is auto-created)
data/

# Editor config and agent skills
.agents/
.cursor/

# Private dev log (contains IPs, server paths, credentials context)
docs/DEVLOG.md
docs/JOURNAL.md

# Stale scaffold (not part of the app)
Update/
